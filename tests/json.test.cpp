#include "../headers/read.h"
#include <iostream>
#include <string>
#include <map>
using namespace std;

// Example usage
int main() 
{   
    map<string, string> map; // Correct spelling
    insert(map); // Pass the map variable to the insert function
    return 0;
}
